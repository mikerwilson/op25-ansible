<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OP25 Channel Control</title>
    <script src="main.js"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #121212;
            color: #ffffff;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            transition: background-color 0.3s, color 0.3s;
            font-family: monospace;
        }
        .page-container {
            padding: 7px;
            display: flex;
            flex-direction: column;
            gap: 0px;
            max-width: 858px;
            width: 100%; /* Ensures it expands to the max width */
            margin: 0 auto; /* Centers the container */
            box-sizing: border-box; /* Ensures padding doesn’t reduce width */
        }

        .audio-player {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: #1e1e1e;
            border-radius: 5px;
            border: 1px solid #333;
            padding: 5px 10px;
            font-family: monospace;

        }
        .status {
            font-size: 18px;
        }
        .stream-start-stop {
            padding: 12px 24px;
            background-color: #1d7831;
            color: #ffffff;
            border: none;
            border-radius: 5px;
            font-size: 20px;
            cursor: pointer;
            padding: 21px;
            font-family: monospace;
        }
        .stream-start-stop.playing {
            background-color: #9a2631;
        }
        .stream-start-stop:hover {
            opacity: 0.9;
        }

        .light-mode .stream-start-stop {
            background-color: #35cd59;
        }

        .light-mode .stream-start-stop.playing {
            background-color: #dc3545;
        }

        .favorites-scanlist {
            background-color: #1e1e1e;
            padding: 5px;
            border-radius: 5px;
            border: 1px solid #333;
            overflow-y: auto;
            font-family: monospace;
        }
        .favorites-scanlist h2 {
            margin-top: 0;
            font-size: 18px;
        }
        .favorites-scanlist div {
            margin-bottom: 5px;
        }
        .favorites-scanlist button {
            display: block;
            width: 100%;
            background-color: #053467;
            color: #fff;
            padding: 18px 10px;
            border-radius: 5px;
            text-align: center;
            border: none;
            font-size: 20px;
            cursor: pointer;
            font-family: monospace;

        }
        .favorites-scanlist button:hover {
            background-color: #007bff;
        }

        .day-night-toggle {
            position: fixed;
            top: 14px;
            right: 10px;
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 24px;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            z-index: 1000;
            font-family: monospace;
        }
        .day-night-toggle:hover {
            background-color: #0056b3;
        }
        .light-mode {
            background-color: #ffffff;
            color: #000000;
        }
        .light-mode .favorites-scanlist, .light-mode .audio-player, .light-mode .channel-list, .light-mode .favorites-scanlist-grid {
            background-color: #f9f9f9;
            border-color: #ccc;
        }
        .light-mode button {
            color: #ffffff;
            background-color: #007bff;
        }
        .light-mode button:hover {
            color: #dddddd;
        }
        .light-mode .scanbutton {
            background-color: #669999;
            color: #ffffff !important;
        }
        .light-mode .scanbutton:hover {
            background-color: #3c5a5a;
        }
        .light-mode .day-night-toggle {
            background-color: #007bff;
            color: #fff;
        }
        .light-mode .day-night-toggle:hover {
            background-color: #0056b3;
        }
        .favorites-scanlist-grid {
            margin-top: 5px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 5px;
        }
        .all-talkgroups {
            background-color: #053467;
            color: white;
            cursor: pointer;
            padding: 17px 20px;
            margin-top: 5px;
            border: none;
            border-radius: 5px;
            text-align: center;
            font-size: 18px;
            outline: none;
            transition: background-color 0.3s;
            font-family: monospace;
        }
        .all-talkgroups:hover {
            background-color: #007bff;
        }
        .channel-list {
            padding: 0 20px;
            display: none;
            overflow: hidden;
            background-color: #1e1e1e;
            border-radius: 5px;
            border: 1px solid #333;
            margin-top: 5px;
            font-family: monospace;

        }
        .channel-list.active {
            display: block;
        }

        .channel-list .favorites-scanlist-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 7px;
            justify-items: center;
        }
        .channel-list button {
            width: 240px;
            padding: 12px 7px;
            background-color: #4b4b4e;
            color: #fff;
            border: none;
            border-radius: 5px;
            font-size: 20px;
            cursor: pointer;
            font-family: monospace;

        }
        .channel-list button:hover {
            background-color: #6c6c73;
        }

        .light-mode .channel-list button {
            background-color: #669999;
        }

        .light-mode .channel-list button:hover {
            background-color: #3c5a5a;
        }

        .nav-bar .flex-container .div_s2 {
            width: 800px
        }

        .search-bar {
            margin-bottom: 20px;
            padding: 10px;
            width: 100%;
            border: 1px solid #333;
            border-radius: 5px;
            font-size: 16px;
            background-color: #1e1e1e;
            color: #fff;
            box-sizing: border-box;
        }

        .search-bar::placeholder {
            color: #aaa;
        }

        .light-mode .search-bar {
            background-color: #f9f9f9;
            color: #000;
            border-color: #ccc;
        }
       .menu-bar {
            display: flex;
            background: #333;
            padding: 0px;
            justify-content: space-around;
            align-items: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            border-bottom: 2px solid #444;
        }

        .menu-bar button {
            background: linear-gradient(to bottom, #555, #333);
            color: #fff;
            font-weight: bold;
            {#font-family: 'Courier New', monospace;#}
            border: 1px solid #666;
            padding: 15px 20px;
            margin: 3px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 18px;
            width: 123px;
            text-shadow: 0 1px 1px rgba(0, 0, 0, 0.5);
            transition: transform 0.2s, box-shadow 0.3s;
        }

        .menu-bar button:hover {
            background: linear-gradient(to bottom, #666, #444);
            transform: scale(1.05);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.4);
        }

        .menu-bar button:active {
            background: #222;
            box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.5);
            transform: scale(0.98);
        }

        .light-mode .menu-bar {
            background-color: #eaeaea
        }

        .light-mode .menu-bar button {
            background: linear-gradient(to bottom, #f7f7f7, #e3e3e3); /* Light gradient */
            color: #333; /* Dark text for readability */
            border: 1px solid #ccc; /* Soft gray border */
            padding: 15px 20px;
            margin: 5px;
            border-radius: 8px;
            cursor: pointer;
            {#font-size: 20px;#}
            width: 123px;
            text-shadow: 0 1px 1px rgba(255, 255, 255, 0.5); /* Subtle highlight */
            transition: transform 0.2s, box-shadow 0.3s;
        }

         .light-mode .menu-bar button:hover {
            background: linear-gradient(to bottom, #e9e9e9, #d6d6d6); /* Slightly darker gradient */
            transform: scale(1.05);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2); /* Lighter shadow */
        }

         .light-mode .menu-bar button:active {
            background: #ccc; /* Active state with solid color */
            box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.3); /* Subtle inset shadow */
            transform: scale(0.98);
        }


        .display-item span {
            font-weight: bold;
        }
        #div_status, #div_plot, #div_about {
            display: block;
        }
        #div_debug {
            display: none;
        }
        .div_status {
            border: 1px;
            border: 2px solid #39FF14;
            color: #39FF14;
            border-radius: 5px;
        }

.       s2 {
            margin: 20px auto;
            padding: 15px;
            background: #000;
            border: 2px solid #39FF14;
            border-radius: 10px;
            color: #39FF14;
            max-width: 600px;
            box-shadow: 0 0 15px rgba(57, 255, 20, 0.5);
        }

        .s2_row {
            display: block;
            flex: 1;
            justify-content: space-between;
            text-align: center;
            align-items: center;
            margin: 0px 0;
            font-size: 30px;
            font-weight: bold;
            height: 35px;
            text-shadow: 0 0 10px rgba(57, 255, 20, 0.8);
        }
        .value {
            color: #39FF14;
            font-size: 30px;
            font-weight: bold;
        }

        .label {
            font-size: 18px;
            color: #FFD700; /* Amber for labels */
            margin-bottom: 5px;
        }

        .light-mode .s2 {
            margin: 20px auto;
            padding: 0px;
            background: #fff;
            /* border: 2px solid #000; */
            border-radius: 10px;
            color: #000;
            /* max-width: 600px; */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
        }

        .light-mode .s2_row {
            display: block;
            flex: 1;
            justify-content: space-between;
            text-align: center;
            align-items: center;
            margin: 0px 0;
            font-size: 30px;
            font-weight: bold;
            height: 35px;
            text-shadow: none; /* Remove glowing effect for better visibility in light mode */
            color: #000; /* Black for text readability */
        }

        .light-mode .value {
            color: #000; /* High contrast black for text */
            font-size: 30px;
            font-weight: bold;
        }

        .light-mode .label {
            font-size: 18px;
            color: #333; /* Dark gray for labels to differentiate from values */
            margin-bottom: 5px;
        }

        .light-mode .div_status {
            border: 0px solid #000; /* High contrast black border */
            color: #000; /* Black text for clear readability */
            background: #fff; /* White background */
            border-radius: 5px;
        }

        .light-mode .s2 {
            margin: 0px auto;
            {#padding: 15px;#}
            background: #fff; /* White background for visibility */
            border: 0px solid #000; /* High contrast black border */
            border-radius: 10px;
            color: #000; /* Black text */
            {#max-width: 600px;#}
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3); /* Subtle shadow for separation */
        }

        .light-mode .s2_row {
            display: block;
            flex: 1;
            justify-content: space-between;
            text-align: center;
            align-items: center;
            margin: 0;
            font-size: 30px; /* Large text for readability */
            font-weight: bold;
            height: 35px;
            color: #000; /* Black text for content */
            text-shadow: none; /* Remove glow for clarity in bright light */
        }

        .light-mode .value {
            color: #000; /* Black for content values */
            font-size: 30px;
            font-weight: bold;
        }

        .light-mode .label {
            font-size: 18px;
            color: #333; /* Dark gray for labels */
            margin-bottom: 5px;
        }

    </style>
</head>
<body onload="do_onload()">
    <button class="day-night-toggle" onclick="toggleTheme()">Day/Night<br>Mode</button>
    <div class="page-container">

    <br>
    <ul class="nav-ul" style="display: none;">
        <li id="li1" class="nav-li"><input id="b1" class="nav-button" type="button" name="B1" value="Home" method="post" action="" onc>        <li id="li2" class="nav-li"><input id="b2" class="nav-button" type="button" name="B2" value="Plot" method="post" action="" onc>        <li id="li3" class="nav-li"><input id="b3" class="nav-button" type="button" name="B3" value="About" method="post" action="" on>        <li id="li4"><span class="copyr"> &copy; 2017-2024 Max H. Parke & Graham J. Norbury [boatbod version]</span></li>
    </ul>
    <div id="div_status" class="div_status">
        <div id="div_s2" class="s2">
            <div class="s2_row">
                <div class="s2_cell_a" style="display: none;"><span class="label">Frequency:</span></div>
                <div class="s2_cell_b1" style="display: none;" id="s2_freq">&nbsp</div>
                <div class="s2_cell_c" id="s2_ch_lbl" style="display:none;"><span class="label">Channel:</span></div>
                <div class="s2_cell_d" id="s2_ch_txt" style="display:none;">&nbsp</div>
                <div class="s2_cell_e" id="s2_ch_dn" style="display:none;"><input type="button" class="chan-button" name="c_dn" value="<" title="Prev Channel" onclick="javascript:f_chan_button(-1);"></div>
                <div class="s2_cell_f" id="s2_ch_dmp" style="display:none;"><input type="button" class="chan-button" name="c_dmp" value="d" title="Log TGs" onclick="javascript:f_dump_button(0);"></div>
                <div class="s2_cell_g" id="s2_ch_up" style="display:none;"><input type="button" class="chan-button" name="c_up>" value=">" title="Next Channel" onclick="javascript:f_chan_button(1);"></div>
            </div>
            <div class="s2_row">
                <div class="s2_cell_a" style="display: none;"><span class="label">Talkgroup:</span></div>
                <div class="s2_cell_b2" class="value" id="s2_tg">Waiting for data...</div>
                <div class="s2_cell_h" id="s2_ch_cap" style="display:none;"><input type="button" class="cap-button" id="cap_bn" name="c_cap" value="start capture" title="Capture" onclick="javascript:f_cap_button(0);"></div>
            </div>
            <div class="s2_row">
                <div class="s2_cell_a" style="display: none;"><span class="label">Group Addr:</span></div>
                <div class="s2_cell_b2" class="value" id="s2_grp">&nbsp</div>
                <div class="s2_cell_h" id="s2_ch_trk" style="display:none;"><input type="button" class="cap-button" id="trk_bn" name="c_trk" value="tracking" title="Tracking" onclick="javascript:f_trk_button(-1);"></div>
            </div>
            <div class="s2_row">
                <div class="s2_cell_a" style="display: none;"><span class="label">Source Addr:</span></div>
                <div class="s2_cell_b" class="value" id="s2_src"><span class="value">&nbsp</span></div>
            </div>
        </div>

    </div>
        <div class="menu-bar" style="display: inline-flex;">
            <button id="btn-hold" data-command="hold" onclick="f_scan_button('hold')">Hold</button>
            <button id="btn-goto" data-command="goto" onclick="f_scan_button('goto')">Goto</button>
            <button id="btn-whitelist" data-command="whitelist" onclick="f_scan_button('whitelist')">Whitelist</button>
            <button id="btn-blacklist" data-command="blacklist" onclick="f_scan_button('lockout')">Blacklist</button>
            <button id="btn-scan" data-command="scan" onclick="setTGIDAndListen('0')">Scan</button>
    </div>
    <br>
    <details>
        <summary>Details</summary>
            <div class="menu-bar">
              <button id="btn-tune-down" data-command="fine-tune-down" onclick="f_tune_button(-100)" title="Fine Tune -100">&lt;&lt;&lt;</button>
              <button id="btn-tune-down" data-command="fine-tune-down" onclick="f_tune_button(-10)" title="Fine Tune -10">&lt;&lt;</button>
              <button id="btn-tune-down" data-command="fine-tune-down" onclick="f_tune_button(-1)" title="Fine Tune -1">&lt;</button>
              <button id="btn-tune-up" data-command="fine-tune-up" onclick="f_tune_button(1)" title="Fine Tune +1">></button>
              <button id="btn-tune-up" data-command="fine-tune-up" onclick="f_tune_button(10)" title="Fine Tune +10">>></button>
              <button id="btn-tune-up" data-command="fine-tune-up" onclick="f_tune_button(100)" title="Fine Tune +100">>>></button>
            </div>
            <div id="div_s1" class="s1">
                 <!-- system freq -->
                Waiting for data...
            </div>
            <br>
            <!-- div_s3 -->
            <div id="div_s3" class="s3">
                <!-- adjacent sites -->
            </div>

            <div id="div_plot"></div>
            <div id="div_about"></div>
    </details>

        <div id="div_debug"></div>
            <img id="img0" style="display: none;">
            <img id="img1" style="display: none;">
            <img id="img2" style="display: none;">
            <img id="img3" style="display: none;">
            <img id="img4" style="display: none;">


        <!-- Audio Player Section -->
        <div class="audio-player">
            <p class="status" id="status">Status: Not playing</p>
            <p class="status" id="timer">--:--</p>
            <button class="stream-start-stop" id="streamButton" onclick="toggleIcecastStream('{{ audio_stream }}')">Start Stream</button>
            <audio id="liveAudio" style="display:none;"></audio>
        </div>

        <!-- Scan Button -->
{#        <button class="scanbutton" onclick="setTGIDAndListen('0')">Scan</button>#}
        <div class="favorites-scanlist-grid">
            <!-- Favorites Section -->
            {% if favorites_tgids | default({}) %}
            <div class="favorites-scanlist">
                <h2>Favorites</h2>
                {% for tgid, name in favorites_tgids.items() %}
                <div>
                    <button onclick="setTGIDAndListen('{{ tgid }}')">{{ name }}<br>{{ tgid }}</button>
                </div>
                {% endfor %}
            </div>
            {% endif %}

            <!-- Whitelisted TGIDs Section -->
            {% if whitelisted_tgids | default({}) %}
            <div class="favorites-scanlist">
                <h2>Scan List</h2>
                {% for tgid, name in whitelisted_tgids.items() %}
                <div>
                    <button onclick="setTGIDAndListen('{{ tgid }}')">{{ name }}<br>{{ tgid }}</button>
                </div>
                {% endfor %}
            </div>
            {% endif %}
        </div>

        <!-- All Talkgroups Section -->
        <button class="all-talkgroups">All Talkgroups</button>
        <div class="channel-list">
            <input type="text" class="search-bar" id="searchTalkgroups" placeholder="Search Talkgroups...">
            <div class="favorites-scanlist-grid" id="talkgroupList">
                {% for tgid, name in channels.items() %}
                <div class="tgid-entry" data-tgid="{{ tgid }}" data-name="{{ name | lower }}">
                    <button onclick="setTGIDAndListen('{{ tgid }}')">{{ name }}<br>{{ tgid }}</button>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>

<script>
    let mediaSource;
    let sourceBuffer;
    const audio = document.getElementById('liveAudio');
    let timerInterval;
    let jumpToLiveEdgeInterval;
    let isJumpToLiveEdgeEnabled = false;
    let jumpIntervalTime = 60000; // Default interval time (60 seconds)

    // Web Audio API variables
    let audioContext;
    let gainNode;
    let gainValue = {{ audio_player_gain }}; // Configurable gain value

    async function toggleIcecastStream(url) {
        const button = document.getElementById('streamButton');
        const status = document.getElementById('status');
        const timer = document.getElementById('timer');

        if (button.classList.contains('playing')) {
            clearInterval(timerInterval);
            clearInterval(jumpToLiveEdgeInterval);
            audio.pause();
            button.textContent = 'Start Stream';
            button.classList.remove('playing');
            status.textContent = 'Status: Not playing';
            timer.textContent = '--:--';
            return;
        }

        try {
            status.textContent = 'Status: Connecting...';
            mediaSource = new MediaSource();
            audio.src = URL.createObjectURL(mediaSource);

            mediaSource.addEventListener('sourceopen', async () => {
                sourceBuffer = mediaSource.addSourceBuffer('audio/mpeg');
                const response = await fetch(url);
                const reader = response.body.getReader();

                async function read() {
                    const { done, value } = await reader.read();
                    if (done) {
                        mediaSource.endOfStream();
                        return;
                    }
                    sourceBuffer.appendBuffer(value);
                    await read();
                }
                await read();
            });

            // Initialize Web Audio API when the stream starts
            initializeAudioContext();

            audio.play();
            button.textContent = 'Stop Stream';
            button.classList.add('playing');
            status.textContent = 'Status: Playing';
            startTimer(timer);

            if (isJumpToLiveEdgeEnabled) {
                startJumpToLiveEdgeInterval();
            }
        } catch (error) {
            status.textContent = 'Status: Error - ' + error.message;
            console.error('Error playing Icecast stream:', error);
        }
    }

    function initializeAudioContext() {
        if (!audioContext) {
            audioContext = new (window.AudioContext || window.webkitAudioContext)();
            const audioSource = audioContext.createMediaElementSource(audio);
            gainNode = audioContext.createGain();
            gainNode.gain.value = gainValue; // Use configurable gain value
            audioSource.connect(gainNode).connect(audioContext.destination);
        }
    }

    function startTimer(timerElement) {
        let seconds = 0;
        timerInterval = setInterval(() => {
            seconds++;
            const minutes = String(Math.floor(seconds / 60)).padStart(2, '0');
            const secs = String(seconds % 60).padStart(2, '0');
            timerElement.textContent = `${minutes}:${secs}`;
        }, 1000);
    }

    function setTGIDAndListen(tgid) {
        const originalPrompt = window.prompt;
        window.prompt = function() { return tgid; };
        f_scan_button('goto');
        window.prompt = originalPrompt;
    }

    function toggleTheme() {
        document.body.classList.toggle('light-mode');
    }

    // Automatically set theme based on system preference
    const prefersDarkScheme = window.matchMedia("(prefers-color-scheme: dark)");
    if (prefersDarkScheme.matches) {
        document.body.classList.remove('light-mode');
    } else {
        document.body.classList.add('light-mode');
    }

    // Collapsible Section Logic
    const collapsible = document.querySelector('.all-talkgroups');
    const content = document.querySelector('.channel-list');

    collapsible.addEventListener('click', function() {
        this.classList.toggle('active');
        content.classList.toggle('active');
    });

    const searchInput = document.getElementById('searchTalkgroups');
    const talkgroupList = document.getElementById('talkgroupList');
    searchInput.addEventListener('input', function () {
        const filter = searchInput.value.toLowerCase();
        const entries = talkgroupList.getElementsByClassName('tgid-entry');

        for (let i = 0; i < entries.length; i++) {
            const entry = entries[i];
            const text = entry.textContent || entry.innerText; // Get all visible text, including multi-line content

            // Check if the text includes the filter (case insensitive)
            entry.style.display = text.toLowerCase().includes(filter) ? '' : 'none';
        }
    });

    // Detect external play, pause, and end events
    audio.addEventListener('play', () => {
        updateUIPlaying();
        if (isJumpToLiveEdgeEnabled) {
            startJumpToLiveEdgeInterval();
        }
    });

    audio.addEventListener('pause', () => {
        updateUIStopped();
        clearInterval(jumpToLiveEdgeInterval);
    });

    audio.addEventListener('ended', () => {
        updateUIStopped();
        clearInterval(jumpToLiveEdgeInterval);
    });

    function updateUIPlaying() {
        const button = document.getElementById('streamButton');
        const status = document.getElementById('status');
        const timer = document.getElementById('timer');

        if (!button.classList.contains('playing')) {
            button.textContent = 'Stop Stream';
            button.classList.add('playing');
            status.textContent = 'Status: Playing';
            startTimer(timer);
        }
    }

    function updateUIStopped() {
        const button = document.getElementById('streamButton');
        const status = document.getElementById('status');
        const timer = document.getElementById('timer');

        clearInterval(timerInterval);
        button.textContent = 'Start Stream';
        button.classList.remove('playing');
        status.textContent = 'Status: Not playing';
        timer.textContent = '--:--';
    }

    // Jump to the live edge of the buffered audio
    function jumpToLiveEdge() {
        if (audio.buffered.length > 0) {
            const liveEdge = audio.buffered.end(audio.buffered.length - 1);
            if (audio.currentTime < liveEdge - 1) {
                audio.currentTime = liveEdge - 0.5;
                console.log('Jumped to live edge:', liveEdge);
            }
        }
    }

    // Toggle the jump to live edge feature
    function toggleJumpToLiveEdge() {
        isJumpToLiveEdgeEnabled = !isJumpToLiveEdgeEnabled;
        if (isJumpToLiveEdgeEnabled) {
            startJumpToLiveEdgeInterval();
        } else {
            clearInterval(jumpToLiveEdgeInterval);
        }
        console.log('Jump to live edge:', isJumpToLiveEdgeEnabled ? 'Enabled' : 'Disabled');
    }

    // Start an interval to run jumpToLiveEdge with configurable time
    function startJumpToLiveEdgeInterval() {
        clearInterval(jumpToLiveEdgeInterval);
        jumpToLiveEdgeInterval = setInterval(jumpToLiveEdge, jumpIntervalTime);
    }

    // Function to update the interval time dynamically
    function setJumpIntervalTime(seconds) {
        jumpIntervalTime = seconds * 1000;
        console.log(`Jump to live edge interval set to ${seconds} seconds.`);
        if (isJumpToLiveEdgeEnabled) {
            startJumpToLiveEdgeInterval();
        }
    }

    if (typeof do_onload !== "function") {
        function do_onload() {
            console.log("Fallback: Page Loaded");
        }
    }
</script>
</body>
</html>
